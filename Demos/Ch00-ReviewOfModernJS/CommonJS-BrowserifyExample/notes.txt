To install Browserify system-wide:
    npm install -g browserify

To set up the dependencies:
    npm install

To bundle the code:
    browserify index.js -o bundle.js

To look at the result:
    Load the test.html in a browser.
