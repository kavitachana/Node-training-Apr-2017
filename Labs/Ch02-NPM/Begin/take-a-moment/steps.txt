Create a package.json file by running:
npm init

Let’s get a package called Moment, put it into package.json
npm install moment --save

notice that node_modules directory is created

then create in same directory timeExample.js with this code:
var moment = require('moment');
console.log(moment().format('MM-DD-YYYY'));

execute using node timeExample.js or leave off the .js extension


